{
    "name": "find-vhdl-entities",
    "displayName": "find-vhdl-entities",
    "description": "Find vhdl entities",
    "version": "0.0.1",
    "engines": {
        "vscode": "^1.46.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:find-vhdl-entities.index",
        "onCommand:find-vhdl-entities.findDesignUnit"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "commands": [
            {
                "command": "find-vhdl-entities.index",
                "title": "Index vhdl files"
            },
            {
                "command": "find-vhdl-entities.findDesignUnit",
                "title": "Find design unit"
            }
        ],
        "configuration": {
            "title": "Find vhdl units",
            "properties": {
                "findVhdlUnits.searchTheseDirectories": {
                    "type": "array",
                    "default": [],
                    "description": "When indexing vhdl files, we will search for files from these directories",
                    "items": {
                        "type": "string",
                        "description": "Directory to index"
                    }
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "yarn run compile",
        "compile": "tsc -p ./",
        "lint": "eslint src --ext ts",
        "watch": "tsc -watch -p ./"
    },
    "devDependencies": {
        "@types/vscode": "^1.46.0",
        "@types/glob": "^7.1.1",
        "@types/mocha": "^7.0.2",
        "@types/node": "^13.11.0",
        "eslint": "^6.8.0",
        "@typescript-eslint/parser": "^2.30.0",
        "@typescript-eslint/eslint-plugin": "^2.30.0",
        "glob": "^7.1.6",
        "mocha": "^7.1.2",
        "typescript": "^3.8.3",
        "vscode-test": "^1.3.0"
    }
}
